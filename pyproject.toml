[project]
name = "vllm-autoresearch"
version = "0.1.0"
description = "Autonomous search for optimal vLLM serving configurations"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "requests>=2.32",
    "vllm>=0.6.0",
]
